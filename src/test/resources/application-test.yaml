spring:
  datasource:
    url: jdbc:mysql://localhost:3306/recipes_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: test
    password: test
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
